## 6️⃣ Producer–Consumer with ThreadPoolExecutor

Build:

* Producer thread generating jobs every 0.5 sec
* Consumer pool processing them

Constraint:

* Use `queue.Queue`
* No busy waiting

Goal:
Test real-world coordination knowledge.
